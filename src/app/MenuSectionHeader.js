export default function MenuSectionHeader({ title }) {
  return (
    <div className="flex flex-row justify-center items-center w-full">
      <h1 className="mt-2.5 -mb-0.5 uppercase">{title}</h1>
    </div>
  );
}
